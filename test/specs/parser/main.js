define((require, exports, module) => {
  'use strict';
  var Parser = require('Parser');
  var ParserCss = require('undefined');

    describe('Parser', () => {

      ParserHtml.run();
      ParserCss.run();

    });
});