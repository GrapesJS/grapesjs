define(function () {
	return {
		stylePrefix		: 'pn-',
		
		// Default panels
		defaults		: [],
		
		// Editor model
		em				: null,
		
		// Delay before show children buttons (in milliseconds)
		delayBtnsShow	: 300,			
	};
});